import React from 'react';
import styles from 'styles/_common.module.scss';

export const DominoPagination = ({ page = 1, totalPage = 5 }) => {
  return <div className={styles.domino_pagination_container}>ddd</div>;
};
